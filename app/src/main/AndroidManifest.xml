<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.statusbar_alexleo.alexstatusbarutils">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CommonToolbarAct" />
        <activity android:name=".ImgStatusAct" />
        <activity android:name=".ScrollToolBarAct" />
        <activity android:name=".ScrollTbAndDlAct" />
        <activity
            android:name=".ScrollingImgActivity"
            android:label="@string/title_activity_scrolling_img"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".FragmentAct"
            android:label="@string/title_activity_fragment" />
        <activity android:name=".TranDrawerStatusAct"></activity>
    </application>

</manifest>